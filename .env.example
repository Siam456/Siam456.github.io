# Analytics IDs. Real values live only in CI/host env (GitHub Actions secrets),
# never in this repo. Each service loads only in a production build when its ID is set.
# For local prod testing, copy to .env.local (gitignored via *.local) and fill in.
VITE_GA_ID=
VITE_CLARITY_ID=
